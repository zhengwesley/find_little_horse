现在，马的位置是固定的，然后会调用assignHorsesToRegions生成马的区域。要求：
马的位置必须在该区域的格子内
每个区域只能分配一匹马
如果分配失败（返回 false），就会打印错误信息并返回 null，导致游戏配置生成失败

如果生成的区域跟马的位置冲突，那么就会导致分配失败。

我想要把这个区域也变成一个固定的参数写在关卡文件中，这样，每一关的各颜色区域不再是根据算法生成，而是已经有固定的位置。

