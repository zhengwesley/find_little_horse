用COCOS Creater 3.8.8 开发一个2d游戏，用纯typescript的脚本实现

玩法：

存在一个N*N的二维网格，生成N个符合规则的小马，目标是把这N只马从网格中找出来

规则：

每种颜色区域内，有且仅有 1 只小马。所以当你发现某个颜色只有一个格子时，那么这个格子肯定是小马。每一行、每一列，有且仅有 1 只小马。因此当我们找到 1 只小马时，我们可以大胆地把它的横竖列都标记为x，就像扫雷一样。小马的上下左右相邻格子，不能有其他小马。顾名思义，就是小马的邻居们，肯定不是小马，因此我们也可以将当前小马格子的 8 方向邻居标记为x。

以上三个基础的规则，除去颜色，数据结构与算法和 N 皇后问题十分相似

玩法就是“扫雷 + 数独约束”混合体

我们先用手工固定的方式把游戏的主体内容脚本做出来

做出一个4*4的网格，横坐标从左到右1234，纵坐标从下到上ABCD，其中有小马的位置是B1，D2，A3，C4，其余位置为空

网格上方显示一些基本信息，例如玩家的初始生命值（为2），该局游戏的游玩时间，剩余找出马的数量（为4）

交互方式：单击标记为X，双击标记为马，标记X可以再次单击撤销。双击进行判定：如果此位置没有马，则扣除1生命值。如果有马，则该位置标记为马，并且剩余马的数量-1

游戏结束判定：当玩家生命值为0，游戏失败。当剩余马的数量为0，游戏胜利。

接下来是要给每个格子着色，存在N（暂定为4），生成随机的N个区域，确保每个区域都包含一直小马。其次，马的位置不再固定，采用回溯算法生成每只小马的位置。

游戏到目前位置，存在的一个问题是很容易碰到配置失败，玩家需要多次点击重新加载来找到正确的配置来开始游戏，

那么，一个重要的技术方向是，要不要把小马位置、颜色位置这两个参数都固定写死在关卡文件中，然后实际玩的时候不再使用算法生成。我应该在此刻做出判断，我觉得写死在关卡文件中是个更加稳定的方式。那么问题是，关卡文件中的这些数据从哪里来呢？这个问题暂时不重要。重要的事情是：