
我想要给当前的游戏添加一个关卡管理系统，要有一个关卡记录文件，文件都记录了每一关的参数

例如

第一关：网格size,小马数量，颜色数量，其实这三个数量应该是一样的，不必要分成三个，例如在4*4的网格中，那么按照游戏的规则约束就需要4匹马，又因为有颜色的约束，每个颜色都有1匹马，那么肯定有四个颜色因此，每一关不同的地方是：每种颜色的数量差异，以及小马的位置，所以可以有的参数是:储存每种颜色数量的数组，例如: [1, 3, 5, 7]。还有合法的小马位置

示例：

{
  "level": 1,
  "gridSize": 4,
  "regionSizes": [1, 3, 5, 7],
  "horsePositions": [
    { "row": 0, "col": 2 },
    { "row": 1, "col": 0 },
    { "row": 2, "col": 3 },
    { "row": 3, "col": 1 }
  ],
  "life": 2
}

有了关卡管理文件之后，需要针对每一关去更改gameconfig，即是说gameconfig里面的相关参数应该是动态加载的。

那么还需要有一个变量去储存一个信息，关卡是第几关。程序需要知道当前应该加载第几关。这个信息应该放在gameconfig中

那么，我们的主控制逻辑应该是：从gameconfig知晓当前处于第几关，然后去关卡管理文件获取当前的关卡参数并加载到gameconfig中

